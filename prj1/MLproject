name: project1

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      p11: {type: float, default: 0.5}
      p12: {type: float, default: 0.1}
      p13: {type: float, default: 0.1}
    command: "python main.py {p11} {p12} {p13}"
